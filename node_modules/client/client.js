const http = require('http');
module.exports = (url, callback) => {
    http.get(url, res => {
        let htmlData = '';
        res.on('data', chunk => {
            htmlData += chunk;
        });
        console.log(htmlData);
        res.on('end', () => {
            callback(null, htmlData);
        });
    });
};